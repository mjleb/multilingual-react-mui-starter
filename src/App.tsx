/**
 * Компонент App, который отображает приветственное сообщение.
 *
 * @returns {JSX.Element} Возвращает JSX, который рендерит строку "hello world".
 */

function App() {
  return <>hello world</>;
}

export default App;
